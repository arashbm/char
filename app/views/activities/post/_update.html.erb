<%= t('activities.post.update_html', actor_name: activity_actor_name(activity.actor),
      verb_suffix: verb_suffix(activity.actor), post_title: link_to(activity.acted.title, activity.acted),
      revision_path: post_revision_path(activity.acted, activity.parameters[:revision_id])) %>
