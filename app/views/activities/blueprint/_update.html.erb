<%= t('activities.post.update', actor_name: activity_actor_name(activity.actor),
      verb_suffix: verb_suffix(@ac), revision_path: post_revision_path(activity.acted, activity.parameters[:revision_id])) %>
